import {StyleSheet,Text,View} from 'react-native';import type {TestResult} from '../../types/compiler';import {COLORS as C} from '../../constants/topics';export function TestResultList({results}:{results:TestResult[]}){return <View>{results.map(r=><View key={r.index} style={s.row}><Text style={[s.status,{color:r.passed?C.success:C.danger}]}>Test {r.index}: {r.passed?'Passed':'Failed'}</Text>{!r.passed&&<Text style={s.detail}>Expected {r.expected}; received {r.actual||'(no output)'}</Text>}</View>)}</View>}const s=StyleSheet.create({row:{paddingVertical:10,borderBottomWidth:1,borderBottomColor:C.line},status:{fontWeight:'800'},detail:{color:C.muted,fontFamily:'monospace',fontSize:11,marginTop:5}})
