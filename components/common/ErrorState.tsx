import {StyleSheet,Text,View} from 'react-native';import {Button} from '../UI';import {COLORS as C} from '../../constants/topics';export function ErrorState({message,onRetry}:{message:string;onRetry?:()=>void}){return <View style={s.wrap}><Text style={s.title}>Something went wrong</Text><Text style={s.text}>{message}</Text>{onRetry&&<Button title="Retry" onPress={onRetry}/>}</View>}const s=StyleSheet.create({wrap:{padding:24,alignItems:'center'},title:{fontWeight:'800',color:C.ink},text:{color:C.muted,textAlign:'center',marginTop:8}})
