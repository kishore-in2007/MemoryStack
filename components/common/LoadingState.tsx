import {ActivityIndicator,StyleSheet,Text,View} from 'react-native';import {COLORS as C} from '../../constants/topics';export function LoadingState({label='Loading...'}:{label?:string}){return <View style={s.wrap}><ActivityIndicator color={C.primary}/><Text style={s.text}>{label}</Text></View>}const s=StyleSheet.create({wrap:{padding:32,alignItems:'center'},text:{marginTop:10,color:C.muted,fontSize:13}})
