import {Pressable,StyleSheet,Text,View} from 'react-native';import {ExternalLink} from 'lucide-react-native';import type {ProblemCatalogItem} from '../../types/problem';import {COLORS as C} from '../../constants/topics';export function ProblemSuggestionRow({item,onPress}:{item:ProblemCatalogItem;onPress:()=>void}){return <Pressable onPress={onPress} style={s.row}><View style={s.content}><Text numberOfLines={2} style={s.title}>{item.title}</Text><Text style={s.meta}>{item.difficulty} · {item.topics.slice(0,2).join(' · ')||item.platform}</Text></View><ExternalLink size={16} color={C.muted}/></Pressable>}const s=StyleSheet.create({row:{minHeight:60,padding:12,borderBottomWidth:1,borderBottomColor:C.line,flexDirection:'row',alignItems:'center'},content:{flex:1},title:{fontWeight:'700',color:C.ink},meta:{fontSize:11,color:C.muted,marginTop:4}})
