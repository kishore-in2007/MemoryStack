import AsyncStorage from '@react-native-async-storage/async-storage';import type {RevisionContext} from '../types/revision';const key=(id:string)=>`revision-context:${id}`;export async function cacheRevisionContext(id:string,value:RevisionContext){await AsyncStorage.setItem(key(id),JSON.stringify(value))}export async function getCachedRevisionContext(id:string){const raw=await AsyncStorage.getItem(key(id));if(!raw)return undefined;try{return JSON.parse(raw) as RevisionContext}catch{return undefined}}
