export function calculateVerifiedScore(testsPassed:number,testsTotal:number,attempts:number,runtimeMs?:number,timeLimitMs=2500){if(testsTotal<=0||testsPassed<testsTotal)return 0;const attemptPenalty=Math.min(20,Math.max(0,attempts-1)*4);const runtimePenalty=runtimeMs===undefined?0:Math.min(10,Math.floor(runtimeMs/timeLimitMs*10));return Math.max(60,100-attemptPenalty-runtimePenalty)}
