import {Stack,router} from 'expo-router';import * as Notifications from 'expo-notifications';import {useEffect} from 'react';import {ActivityIndicator,View} from 'react-native';import {useStore} from '../store/useStore';import {registerForNotifications} from '../lib/notifications';import {todayInTimeZone} from '../lib/dateOnly';import {COLORS} from '../constants/topics';
export default function RootLayout(){const hydrated=useStore(s=>s.hydrated),hydrate=useStore(s=>s.hydrate),revisions=useStore(s=>s.revisions),hour=useStore(s=>s.notificationHour);useEffect(()=>{hydrate()},[]);useEffect(()=>{if(!hydrated)return;const today=todayInTimeZone(),due=revisions.filter(x=>x.status!=='completed'&&x.due_date<=today);registerForNotifications(hour,due.length,due[0]?.id).catch(()=>{});const sub=Notifications.addNotificationResponseReceivedListener(response=>{const id=response.notification.request.content.data?.revisionId;if(typeof id==='string')router.push(`/revision/${id}/review`);else router.push('/(tabs)/queue')});return()=>sub.remove()},[hydrated,hour,revisions]);if(!hydrated)return <View style={{flex:1,alignItems:'center',justifyContent:'center',backgroundColor:COLORS.bg}}><ActivityIndicator size="large" color={COLORS.primary}/></View>;return <Stack screenOptions={{headerStyle:{backgroundColor:'#fff'},headerTintColor:COLORS.ink,headerShadowVisible:false}}><Stack.Screen name="index" options={{headerShown:false}}/><Stack.Screen name="(auth)" options={{headerShown:false}}/><Stack.Screen name="(tabs)" options={{headerShown:false}}/><Stack.Screen name="revision/[id]" options={{headerShown:false}}/></Stack>}
