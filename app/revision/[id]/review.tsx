import {Linking,ScrollView,Text} from 'react-native';import {router,useLocalSearchParams} from 'expo-router';import {Button,Pill,ui} from '../../../components/UI';import {RevisionProgressHeader} from '../../../components/revision/RevisionProgressHeader';import {PreviousSolutionCard} from '../../../components/revision/PreviousSolutionCard';import {LoadingState} from '../../../components/common/LoadingState';import {ErrorState} from '../../../components/common/ErrorState';import {useRevisionContext} from '../../../hooks/useRevisionContext';import {markRevisionReviewed} from '../../../services/revisionService';import {useState} from 'react';
export default function Review(){const {id}=useLocalSearchParams<{id:string}>(),{data,loading,error,retry}=useRevisionContext(id),[saving,setSaving]=useState(false);if(loading)return <LoadingState label="Loading your previous work..."/>;if(error||!data)return <ErrorState message={error??'Revision not found.'} onRetry={retry}/>;const next=async()=>{setSaving(true);try{await markRevisionReviewed(id);router.push(`/revision/${id}/practice`)}catch{}finally{setSaving(false)}};const p=data.originalProblem;return <ScrollView style={ui.screen} contentContainerStyle={ui.content}><RevisionProgressHeader step={1}/><Pill text={`REVISION ${data.revision.revisionNumber} OF 10`}/><Text style={ui.title}>{p.name}</Text><Text style={ui.subtitle}>{p.platform} · {p.difficulty} · {p.topic} / {p.subtopic}</Text>{p.pattern&&<Text style={ui.subtitle}>Pattern: {p.pattern}</Text>}{p.userNotes&&<Text style={ui.subtitle}>{p.userNotes}</Text>}{p.url&&<Button outline title="Open original problem" onPress={()=>Linking.openURL(p.url!)}/>}<Text style={[ui.label,{marginTop:26}]}>MOST RECENT SUCCESSFUL SOLUTION</Text><PreviousSolutionCard solution={data.previousSolution}/><Button title={saving?'Recording review...':'Reviewed - Continue'} disabled={saving} onPress={next}/></ScrollView>}
